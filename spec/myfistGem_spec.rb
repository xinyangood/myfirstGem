require 'spec_helper'

describe MyfistGem do
  it 'has a version number' do
    expect(MyfistGem::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
